This respority is use store shell scripts.
git usage:

git init
git add
git commit
git status  #check file status
git diff    #check file changed what content.
git log     #back the past version
git reflog  #go to the future
git reset --hard HEAD^
